export const USDollar = new Intl.NumberFormat("en-US", {
  style: "currency",
  currency: "USD",
});

export const UYUPeso = new Intl.NumberFormat("en-US", {
  style: "currency",
  currency: "UYU",
});
